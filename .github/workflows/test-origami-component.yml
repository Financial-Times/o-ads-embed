git stash; git checkout master && git pull origin master && pbpaste > ".github/workflows/test-origami-component.yml" && git add .github/workflows/test-origami-component.yml && git commit -m "Add missing env to bring back bundle-size-cli PR comment" && git push origin master