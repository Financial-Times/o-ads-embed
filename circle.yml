machine:
  node:
    version: 6
dependencies:
  override:
    - npm install -g origami-build-tools@^7
    - obt install
  cache_directories:
    - "node_modules"
test:
  override:
    - obt verify
    - obt test
