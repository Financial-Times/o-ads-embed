machine:
  node:
    version: 6
dependencies:
  override:
    - npm install
    - ./node_modules/.bin/obt install
  cache_directories:
    - "node_modules"
test:
  override:
    - npm test
    - ./node_modules/.bin/obt verify
